import Loading from "@/app/components/shared/Loading"

const loading = () => {
  return (
    <section className="h-[15vh] flex justify-center">
      <Loading />
    </section>
  )
}

export default loading